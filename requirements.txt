# Supply Chain Intelligence Co-Pilot — Python dependencies
# Covers: ai-service (FastAPI + LangGraph), mcp-server, mock-apis
# See docs/02_technical_requirements.md Section 2 for the full stack rationale.

# --- AI service host ---
fastapi>=0.115
uvicorn[standard]>=0.52.1
pydantic>=2.9

# --- Agent orchestration ---
langgraph>=0.2
langchain>=0.3
langchain-core>=0.3

# --- MCP tool connectivity ---
# Floor is the fix version for PYSEC-2026-1617 (DNS rebinding), CVE-2026-52869
# (session hijack) and CVE-2026-59950 (WebSocket transport) — see mcp_server/server.py.
mcp>=1.28.1

# --- Knowledge Base / RAG ---
chromadb>=0.5
sentence-transformers>=3.2

# --- Relational database access ---
sqlalchemy>=2.0
psycopg[binary]>=3.2

# --- Text-to-SQL validation ---
sqlglot>=25.0

# --- Mock enterprise APIs / HTTP client ---
httpx>=0.27

# --- Config & environment ---
python-dotenv>=1.0

# --- Observability ---
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0

# --- Testing ---
pytest>=8.3
pytest-asyncio>=0.24

# --- Lint / format / type checking ---
ruff>=0.15
mypy>=2.3
